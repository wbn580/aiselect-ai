---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';
import CodexReferralCard from '../components/CodexReferralCard.astro';

const articles = (await getCollection('articles')).filter((a) => !a.data.hideFromHome);
const sorted = articles.sort((a, b) =>
  new Date(b.data.publishDate || b.data.pubDatetime || '').getTime() - new Date(a.data.publishDate || a.data.pubDatetime || '').getTime()
);
---

<BaseLayout
  title="Choose Better AI Tools. Automate the Right Workflow."
  description="Independent AI tool reviews for better software decisions, plus a free SecureFlow Snapshot for screening what to automate next."
>
  <!-- Hero -->
  <section class="relative overflow-hidden bg-[#1A1A2E]">
    <div class="max-w-6xl mx-auto px-6 py-16 sm:py-20 lg:py-24">
      <div class="w-full">
        <p class="text-[11px] font-mono font-bold uppercase tracking-[.16em] text-[#83e9dd]">
          Independent reviews · free workflow screening
        </p>
        <h1 class="mt-4 text-4xl sm:text-5xl lg:text-6xl font-mono font-bold text-white leading-[1.08] tracking-tight">
          Choose the right AI tools.
          <span class="block mt-2 text-[#83e9dd]">Automate the right workflow.</span>
        </h1>
        <p class="mt-6 text-base sm:text-lg text-gray-300 max-w-3xl leading-relaxed">
          AI Select helps developers, creators, and founders choose software through hands-on reviews. SecureFlow gives Australian and cross-border SMEs a free, server-verified route for one low-risk workflow.
        </p>
        <div class="mt-8 grid gap-3 sm:flex sm:flex-wrap">
          <a href="#reviews" class="inline-flex min-h-12 items-center justify-center gap-2 rounded-xl bg-[#00D9C0] px-6 py-3 font-mono text-sm font-bold text-[#1A1A2E] transition-all hover:-translate-y-0.5 hover:bg-[#8be9de] focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-[#83e9dd]">
            Browse AI tool reviews ↓
          </a>
          <a href="/services/secureflow/" class="inline-flex min-h-12 items-center justify-center gap-2 rounded-xl bg-white px-6 py-3 font-mono text-sm font-bold text-[#1A1A2E] transition-all hover:-translate-y-0.5 hover:bg-[#e8fbf8] focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-white">
            Run the free workflow Snapshot →
          </a>
        </div>
        <p class="mt-5 text-xs leading-relaxed text-gray-500">
          Independent reviews by Selector Labs · SecureFlow operated by Arrivau Pty Ltd
        </p>
      </div>
    </div>
  </section>

  <div class="max-w-6xl mx-auto px-6">
    <CodexReferralCard placement="home" />
  </div>

  <!-- Separately disclosed SecureFlow free launch area -->
  <section class="max-w-6xl mx-auto px-6 mt-14" aria-labelledby="secureflow-heading">
    <div class="relative overflow-hidden rounded-2xl border border-[#dce8e6] bg-[#171a33] px-6 py-8 sm:px-9 sm:py-10 text-white">
      <div class="absolute inset-0 opacity-60 bg-[radial-gradient(circle_at_85%_0%,rgba(0,217,192,.27),transparent_34%)] pointer-events-none"></div>
      <div class="relative grid lg:grid-cols-[1fr_auto] gap-8 items-end">
        <div class="max-w-2xl">
          <p class="text-[11px] font-mono font-bold uppercase tracking-[.16em] text-[#83e9dd]">Free self-serve launch · editorially separate</p>
          <h2 id="secureflow-heading" class="mt-3 text-2xl sm:text-3xl font-mono font-bold tracking-tight">SecureFlow: screen one low-risk workflow.</h2>
          <p class="mt-4 text-sm sm:text-base leading-relaxed text-[#c8ccdc]">Run the free rules-based Snapshot, receive an immediate server-verified route, then use the self-serve definition kit. Paid implementation is planned but not open for order, and any future purchase could not change an AI Select review.</p>
        </div>
        <a href="/services/secureflow/" class="inline-flex min-h-12 items-center justify-center rounded-xl bg-[#00d9c0] px-6 py-3 font-mono text-sm font-bold text-[#171a33] hover:bg-[#8be9de] transition-colors">Run the free snapshot →</a>
      </div>
    </div>
  </section>

  <!-- Latest Reviews -->
  <section id="reviews" class="max-w-6xl mx-auto px-6 mt-16">
    <div class="flex items-center justify-between mb-8">
      <h2 class="text-2xl font-mono font-bold tracking-tight">Latest Reviews</h2>
    </div>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-5">
      {sorted.slice(0, 6).map(a => {
        const slug = a.id.replace(/\.md$/, '');
        const date = new Date(a.data.publishDate || a.data.pubDatetime || '').toLocaleDateString('en-US', { month: 'short', year: 'numeric' });
        return (
          <a href={`/${slug}/`} class="group block bg-white border border-gray-200 rounded-xl p-5 hover:border-[#00D9C0] hover:shadow-md transition-all">
            <div class="flex items-center justify-between mb-3">
              <span class="text-2xs font-mono uppercase tracking-wider text-[#00D9C0] bg-[#00D9C0]/5 px-2 py-1 rounded">
                {a.data.category.replace(/-/g, ' ')}
              </span>
              {a.data.rating && (
                <span class="text-xs font-mono text-[#6B7280] tabular-nums">{a.data.rating}/10</span>
              )}
            </div>
            <h3 class="font-mono font-bold text-base leading-snug group-hover:text-[#00D9C0] transition-colors mb-2">
              {a.data.title}
            </h3>
            <p class="text-sm text-[#6B7280] leading-relaxed line-clamp-2 mb-3">{a.data.description}</p>
            <span class="text-2xs text-gray-400">{date}</span>
          </a>
        );
      })}
    </div>
  </section>
</BaseLayout>
